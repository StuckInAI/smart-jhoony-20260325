version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/database/calculator.db
    volumes:
      - ./database:/app/database
    command: node server.js
